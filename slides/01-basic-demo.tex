\section{The basics of building}

\subsection{Simple program}
\begin{frame}[fragile]
    \frametitle{\secname: \small\subsecname\normalsize}
    \begin{columns}

    % Left side
    \column{0.4\textwidth}
        \onslide<1->{Let's say you have a simple program and you want
        to compile it...} \\~\\

        \onslide<3->{\texttt{gcc dummy\_main.c -o dummy\_main}} \\~\\

        \onslide<3->{Pretty simple, right?}

    % Right side
    \column<2->{0.6\textwidth}
        \small
        \begin{lstlisting}[language=C]
// dummy_main.c
int
main(int argc, char *argv[])
{
    return 0;
}
        \end{lstlisting}
        \normalsize

    \end{columns}
\end{frame}

\subsection{Slower demo (demo01)}
\begin{frame}{\secname: \small\subsecname\normalsize}
    But let's say we have a bigger project... \pause

    \begin{itemize}
        \item \texttt{data.c}: 1 MB of static data (because of reasons)
        \item \texttt{main.c}: Do some stuff with the static data
        \item \texttt{*\_make.sh}: Helper scripts for building the demo
    \end{itemize}
\end{frame}

\begin{frame}{\secname: \small\subsecname\normalsize}
    Let's go live!

    % Steps:
    %  1. run slow_make.sh (builds slowly)
    %  2. make a change
    %  3. run slow_make.sh again (builds slowly)
    %  4. make another change
    %  5. run fast_make.sh (builds slowly)
    %  6. make the final change
    %  7. run fast_make.sh (builds fast)
\end{frame}

\subsection{Slower demo (afterthoughts)}
\begin{frame}{\secname: \small\subsecname\normalsize}
    \begin{columns}

    % Left side
    \column{0.5\textwidth}
        Essentially, Makefile solves that slow building issues. \\~\\

        However, Makefiles can also solve (or create) other issues.
        What if... \\~\\ \pause

        \begin{itemize}
            \item There were a lot files? In multiple directories?
            \item There were multiple targets?
            \item The project needed specific flags?
        \end{itemize}

    % Right side
    \column{0.5\textwidth}
        % TODO Add image
    \end{columns}
\end{frame}
